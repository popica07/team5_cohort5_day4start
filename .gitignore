# Maven build output — regenerated by ./mvnw, must never be committed.
# Tracked .class files are binary and change on every build, which makes them
# unmergeable and a constant source of conflicts between branches.
backend/target/
target/

# macOS
.DS_Store

# IDE
.idea/
*.iml
.env
